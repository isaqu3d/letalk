{
  "name": "letalk",
  "version": "0.0.0",
  "private": true,
  "description": "Plataforma de enriquecimento de leads a partir de CNPJ — teste técnico Letalk",
  "type": "module",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "lint": "echo \"lint não configurado ainda\" && exit 0",
    "test": "echo \"test não configurado ainda\" && exit 0",
    "build": "echo \"build não configurado ainda\" && exit 0"
  }
}
